[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "resource-monitor"
version = "1.1.1"
description = "CLI resource monitoring tool"
requires-python = ">=3.9"
dependencies = [
    "psutil"
]

[project.scripts]
resource-monitor = "app.cli:main"